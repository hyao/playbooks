#first playbook to run on any node to deploy
#1. it need to be run once
#2. deploy ansible user
#3. set up sshd
#need to change ansible inventory after this playbook is run

---
- hosts: aws2
  sudo: yes

  tasks:
  - include: roles/bootstrap/tasks/main.yml

  handlers:
  - include: roles/bootstrap/handlers/main.yml
